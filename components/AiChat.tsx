'use client';

import React from 'react';

export default function AiChat({ adTitle }: { adTitle: string }) {
  return (
    <div style={{ padding: '20px', textAlign: 'center', color: '#666', border: '1px dashed #ccc', borderRadius: '8px', backgroundColor: '#f8f9fa' }}>
      <p>AI Chat Placeholder (Module Disabled)</p>
    </div>
  );
}